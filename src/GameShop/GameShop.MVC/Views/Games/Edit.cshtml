@model GameShop.MVC.ViewModels.GameViewModel

@{
    ViewData["Title"] = "Izmeni Igru";
}

<link rel="stylesheet" href="~/css/games.css" />

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">

            <div class="glass-panel p-5">

                <div class="d-flex align-items-center mb-4">
                    <h2 class="page-header-title">
                        <i class="bi bi-pencil-square me-2"></i> Izmeni Igru
                    </h2>
                </div>

                <form asp-action="Edit">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                    <input type="hidden" asp-for="Id" />

                    <div class="mb-4">
                        <label asp-for="Title" class="form-section-label">Naslov Igre</label>
                        <input asp-for="Title" class="form-control form-control-lg" placeholder="Unesite naziv igre..." />
                        <span asp-validation-for="Title" class="text-danger small"></span>
                    </div>

                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label asp-for="Genre" class="form-section-label">Žanr</label>
                            <input asp-for="Genre" class="form-control" placeholder="npr. RPG" />
                            <span asp-validation-for="Genre" class="text-danger small"></span>
                        </div>
                        <div class="col-md-6">
                            <label asp-for="Price" class="form-section-label">Cena (RSD)</label>
                            <input asp-for="Price" class="form-control" type="number" step="0.01" />
                            <span asp-validation-for="Price" class="text-danger small"></span>
                        </div>
                    </div>

                    <div class="row mb-4">
                        <div class="col-md-6">
                            <label asp-for="ReleaseDate" class="form-section-label">Datum Izlaska</label>
                            <input asp-for="ReleaseDate" class="form-control" type="date" />
                            <span asp-validation-for="ReleaseDate" class="text-danger small"></span>
                        </div>
                        <div class="col-md-6">
                            <label asp-for="CoverImage" class="form-section-label">Naziv Slike (npr. game.jpg)</label>
                            <input asp-for="CoverImage" class="form-control" placeholder="ime_fajla.jpg" />
                            <span asp-validation-for="CoverImage" class="text-danger small"></span>
                        </div>
                    </div>

                    <div class="mb-5">
                        <label asp-for="Description" class="form-section-label">Opis</label>
                        <textarea asp-for="Description" class="form-control" rows="5" placeholder="Detaljan opis igre..."></textarea>
                        <span asp-validation-for="Description" class="text-danger small"></span>
                    </div>

                    <div class="d-flex gap-3 pt-3">
                        <button type="submit" class="btn btn-primary fw-bold px-4 py-2 flex-grow-1">
                            <i class="bi bi-save me-2"></i> SAČUVAJ IZMENE
                        </button>

                        <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-light px-4 py-2">
                            Odustani
                        </a>
                    </div>
                </form>

            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}